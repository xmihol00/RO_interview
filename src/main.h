#ifndef _MAIN_H_ // use guards instead of #pragma once, which is non-standard and older compilers may not support it
#define _MAIN_H_

#include "assignment.h"
#include "AlignedAllocator.hpp"
#include "trie.h"

#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <omp.h>

#endif // _MAIN_H_